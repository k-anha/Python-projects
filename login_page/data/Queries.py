from data.data_types import *

# Defining Queries
QUERY_TO_INSERT_VALUES: Query = (
    """ INSERT INTO CREDENTIALS (USERNAME, PASSWORD, EMAIL) VALUES(?,?,?) """
)
QUERY_TO_DELETE_VALUES: Query = (
    """ DELETE FROM CREDENTIALS WHERE EMAIL = ? AND PASSWORD = ? """
)
QUERY_TO_LOGIN_USER: Query = (
    """ SELECT EMAIL, PASSWORD FROM CREDENTIALS WHERE EMAIL = ? AND PASSWORD = ? """
)
